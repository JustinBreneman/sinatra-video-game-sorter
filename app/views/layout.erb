<!DOCTYPE html>
<html>
    <head>
        <title>
            Online Video Game Database
        </title>
        <style>
            .plain_link {
                text-decoration: none;
                color: black;
            }

            button a {
                text-decoration: none;
                color: black;
            }

            body {
                background-color: lightblue;
            }

            header {
                border-style: outset;
                border-color: lightblue;
            }
        </style>
    </head>
    <body>
        <header>
            <h1><a href='/' class='plain_link'>Welcome!</a></h1>
            <% if Helpers.is_logged_in?(session) %>
                <button><a href='/logout'>Logout</a></button>
            <% else %>
                <button><a href='/signup'>Sign Up</a></button>
                <button><a href='/login'>Login</a></button>
            <% end %>
        </header>
        <br>
        <br>
        <br>
        <%= yield %>
    </body>
</html>